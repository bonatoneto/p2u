import { styled } from "styled-components"

export const ValidatePassword = styled.div`
  padding: 2.1vh;
  display: flex;
  flex-direction: column;
  gap: 0.6vh;
  margin-bottom: 1vh;
`

export const Title = styled.p`
  margin-bottom: 1vh;
  font-weight: 500;
  font-size: var(--font-size-m);
`

export const View = styled.div`
  display: flex;
  align-items: center;
  gap: 1vh;
`

export const Text = styled.p`
  font-weight: 400;
  color: var(--gray);
  font-size: var(--font-size-m);
  margin-bottom: 0;
`
